<h1>Stations</h1>

<ul>
  <% @stations.each do |station| %>
    <li><%= link_to station.name, station_path(station) %></li>
  <% end %>
</ul>
<hr>
<div class="new">
  <button class="hide_buttons btn">New</button>
  <div class="hidden_form">
    <%= render 'form' %>
 </div>
</div>
